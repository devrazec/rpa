{/* <a
                                href={image.image_url.startsWith("http") ? image.image_url : `https://${image.image_url}`}
                                target="_blank"
                                rel="noopener noreferrer"
                                style={{
                                    display: "flex",
                                    flexDirection: "column",
                                    alignItems: "center",
                                    textAlign: "center"
                                }}
                            >
                                <img
                                    src={image.image_url}
                                    alt={`${image.subcategory}-${image.filename}`}
                                    style={{
                                        width: "200px",
                                        //height: "50px",
                                        objectFit: "cover",
                                        cursor: "pointer"
                                    }}
                                />
                                <span style={{ marginTop: "8px", fontSize: "14px", color: "#555" }}>
                                    {image.category} / {image.subcategory}
                                </span>
                            </a> */}